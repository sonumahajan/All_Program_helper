#include <bits/stdc++.h>
 
 
 
 
using namespace std;
 
 
 
 
int main()
 
{
 
    long long n,m,w=1;
 
    long int p=pow(10,9)+7;
 
    cin >> n;
 
    cin >> m;
 
    while(m>0)
 
    {
 
        w=(w*m)%p;                          // or can use w=fmod((w*m),p)
 
        m--;
 
    }
 
    cout << w << "\n";
 
    return 0;
 
}
Language: C++17
